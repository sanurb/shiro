---
"@sanurb/shiro-cli": minor
---

feat: add a versioned judged-corpus benchmark and rebuild-integrity command

`shiro benchmark` validates frozen source identities, licenses, adjudicated graded judgments, evidence locators, hardware profiles, and reviewed thresholds. It reports retrieval metrics, paired confidence intervals, latency percentiles, RSS, explain completeness, determinism, and mandatory post-rebuild ranking integrity without claiming insufficient fixtures pass the release gate.

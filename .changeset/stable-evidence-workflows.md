---
"@sanurb/shiro-cli": minor
---

Add stable `blk_` evidence handles with explicit supersession, deferred block reads, page-scoped reads, and deduplicated multi-query search packs. Search and explain now expose stable evidence identity independently from immutable result snapshots and operational segments.
